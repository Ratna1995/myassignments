#include<stdio.h>
#include<stdlib.h>
#define MAX 64

int strl(const char *str);

int rotstr(const char *str1,const char *str2);

void strcomp(char *sbuf,char *dbuf);

int strspn(const char *buf1,const char *buf2);
