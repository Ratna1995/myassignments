#include<stdio.h>
#include<stdlib.h>
#include<string.h>

#define MAX 30		//macro which replaces 30 by MAX

char* snappend(char *, char *, int);/*function prototype to append n number of 
							characters*/
int strleng(char *);	//function prototype to find string length

int atoint(char ch[]);	//function prototype to convert alphabets into integer
