#include<stdio.h>
#include<stdlib.h>
#include<string.h>

#define MAX 30	//macro for MAX size

/* function prototypes for inserting character, string length and atoi*/
char* insertchar(char *str, const char ch, int pos);
int strleng(char *);
int my_atoi(char n[]);
