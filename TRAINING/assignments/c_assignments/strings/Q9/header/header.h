#include<stdio.h>
#include<stdlib.h>
#include<string.h>

#define MAX 30	//macro for MAX

/* function prototype to parse the string until the delimiter is found*/
//char* str_tok(const char *, const char *);
